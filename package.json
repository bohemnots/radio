{
  "name": "bohemnots",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client": "yarn workspace client",
    "server": "yarn workspace server"
  },
  "devDependencies": {
    "eslint": "^7.2.0"
  }
}
