{
  "name": "bohemnots",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client": "yarn workspace client",
    "server": "yarn workspace server"
  },
  "devDependencies": {
    "eslint": "^6.8.0"
  }
}
